version: '3.9'
services:
  app:
    build: .
    ports:
      - '4000:4000'
    volumes:
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
      - PORT=4000
      - FRONTEND_URL=http://localhost:4000
      - SESSION_SECRET=super-secret-change-me
